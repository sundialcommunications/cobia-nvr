disk space
uptime/load
network usage
overall expected disk usage per day
authentication
make entire image link to cam page
add bottom <head> insert in config
add bottom <body> insert in config
move camera name on top of image on individual cam page
